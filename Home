<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>ProHelmet — Smart Helmet Kiosk</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    /* --- Base (dark theme) --- */
    :root {
      --bg: #0b0b0f;
      --text: #e8e8e8;
      --muted: #b6b6b6;
      --accent: #7cc5ff;     /* link color */
      --divider: #26262b;
      --card: #121217;
    }

    html, body {
      height: 100%;
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      color: var(--text);
      background: var(--bg);
      font-family: "Georgia Pro", Georgia, "Times New Roman", serif;
      line-height: 1.6;
    }

    .container {
      max-width: 960px;
      margin: 0 auto;
      padding: 20px 16px 64px;
    }

    /* --- Header logo --- */
    header {
      text-align: center;
      padding: 32px 0 12px;
    }
    header img {
      max-width: 360px;       /* adjust as you like */
      width: 75%;
      height: auto;
      display: inline-block;
      filter: drop-shadow(0 6px 18px rgba(0,0,0,.45));
    }

    /* --- Nav --- */
    .nav {
      position: sticky;
      top: 0;
      z-index: 10;
      background: linear-gradient(180deg, rgba(11,11,15,0.95), rgba(11,11,15,0.85));
      backdrop-filter: blur(6px);
      border-top: 1px solid var(--divider);
      border-bottom: 1px solid var(--divider);
    }
    .nav-inner {
      max-width: 960px;
      margin: 0 auto;
      padding: 12px 16px;
      text-align: center;
      font-size: 1.05rem;
    }
    .nav a {
      color: var(--accent);
      text-decoration: none;
      padding: 6px 10px;
      border-radius: 999px;
      margin: 0 6px;
    }
    .nav a:hover, .nav a:focus {
      background: #1a1a22;
      outline: none;
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .divider {
      color: var(--muted);
      margin: 0 2px;
      user-select: none;
    }

    /* --- Hero animation --- */
    .hero {
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 24px 0 8px;
    }
    .hero img {
      width: min(860px, 100%);
      height: auto;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,.55);
      background: var(--card);
    }

    /* --- Sections --- */
    section {
      scroll-margin-top: 84px;   /* so anchors aren’t hidden under sticky nav */
      padding: 28px 0;
      border-bottom: 1px solid var(--divider);
    }
    h2 {
      margin: 0 0 10px;
      font-size: clamp(1.3rem, 1rem + 1.2vw, 1.6rem);
    }
    p, li {
      color: var(--text);
    }
    .muted { color: var(--muted); }

    /* --- Footer --- */
    footer {
      text-align: center;
      padding: 26px 0 10px;
      color: var(--muted);
      font-size: .95rem;
    }

    /* --- Small screens --- */
    @media (max-width: 560px) {
      header img { width: 85%; max-width: 420px; }
      .nav-inner { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <div class="container">

    <!-- Header logo (update src to your uploaded path) -->
    <header>
      <!-- Example: put the file in /assets and replace src below -->
      <img src="assets/ProHelmet3.png" alt="ProHelmet logo">
    </header>

  </div>

  <!-- Sticky navigation -->
  <nav class="nav" aria-label="Primary">
    <div class="nav-inner">
      <a href="#about">About</a><span class="divider">|</span>
      <a href="#privacy">Privacy</a><span class="divider">|</span>
      <a href="#terms">Terms</a><span class="divider">|</span>
      <a href="#refunds">Refunds</a><span class="divider">|</span>
      <a href="#contact">Contact</a>
    </div>
  </nav>

  <div class="container">

    <!-- Hero animation (update src to your uploaded path) -->
    <div class="hero" aria-label="Demonstration">
      <img src="assets/animation1.gif" alt="ProHelmet demonstration animation">
    </div>

    <!-- Sections -->
    <section id="about">
      <h2>About</h2>
      <p>ProHelmet is a smart, self-service helmet kiosk. It detects helmet placement with industrial sensors, handles UPI/card payments via Razorpay, and offers a live dashboard for operators.</p>
      <p class="muted">Built for shops, campuses, and workplaces to drive helmet compliance without staff intervention.</p>
    </section>

    <section id="privacy">
      <h2>Privacy</h2>
      <p>We collect only what’s needed to operate the kiosk and process payments (e.g., transaction details). Payments are processed by Razorpay per their policies.</p>
      <p>To access, correct, or delete your data, contact us at <a href="mailto:support@example.com">support@example.com</a>.</p>
    </section>

    <section id="terms">
      <h2>Terms</h2>
      <ul>
        <li>Use the kiosk as intended. Do not tamper with hardware or software.</li>
        <li>Payments are processed securely by Razorpay. Provide accurate information.</li>
        <li>Service is provided “as is” to the extent permitted by law.</li>
      </ul>
    </section>

    <section id="refunds">
      <h2>Refunds</h2>
      <p>All payments are final once a service is delivered. If you were charged but service failed due to a technical error, contact us within 24 hours with the payment reference.</p>
      <p>Email: <a href="mailto:billing@example.com">billing@example.com</a></p>
    </section>

    <section id="contact">
      <h2>Contact</h2>
      <p><strong>Email:</strong> <a href="mailto:support@example.com">support@example.com</a><br>
         <strong>Phone:</strong> +91-XXXXXXXXXX<br>
         <strong>Address:</strong> Your business address</p>
    </section>

    <footer>
      © <span id="y"></span> ProHelmet. All rights reserved.
    </footer>
  </div>

  <script>
    // current year
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>
